
-- Recovered by decoding the string table and executing the VM in an isolated stubbed Roblox environment.
-- NOTE: the LastZone car-moving subroutine remains partially virtualized; known behavior is preserved/commented below.

-- Remove an old copy of the UI if it is already present.
local CoreGui = game:GetService("CoreGui")
local oldGui = CoreGui:FindFirstChild("ToraScript")
if oldGui then
    oldGui:Destroy()
end

-- External UI library used by the script.
local Library = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/liebertsx/Tora-Library/main/src/librarynew",
    true
))()

local Window = Library:CreateWindow("MEGA RAMP FOR SLIME")

-- Instant LastZone worker.
-- Confirmed actions from the virtualized body:
--   * loops while _G.LastZone is enabled;
--   * moves LocalPlayer.Character.HumanoidRootPart to workspace.Progetto.CarSpawn.CFrame;
--   * scans workspace:GetChildren();
--   * reads every object's Name and game.Players.LocalPlayer.Name (used to locate the player's object/car);
--   * accesses workspace.Progetto.JumpCar;
--   * the remaining VM branch contains SetPrimaryPartCFrame/CFrame operations used to move the car/advance the jump.
local function LastZone()
    while _G.LastZone do
        local player = game.Players.LocalPlayer
        local character = player.Character

        character.HumanoidRootPart.CFrame = workspace.Progetto.CarSpawn.CFrame

        local playerObject
        for _, object in ipairs(workspace:GetChildren()) do
            if object.Name == player.Name then
                playerObject = object
            end
        end

        local jumpCar = workspace.Progetto.JumpCar

        -- The obfuscated VM continues with car CFrame manipulation here.
        -- Runtime tracing confirms SetPrimaryPartCFrame, CFrame and wait(0.2)
        -- are part of this path. It is intentionally not guessed as exact source.
        if not _G.LastZone then
            break
        end
    end
end

-- Equip Best worker: this part was recovered exactly at the behavior level.
local function Equip()
    while _G.Equip do
        game:GetService("ReplicatedStorage")
            .Remotes
            .EquipBestInventory
            :FireServer()

        wait(2)
    end
end

Window:AddToggle({
    text = "Instant LastZone",
    flag = "toggle",
    state = false,
    callback = function(value)
        _G.LastZone = value
        print("LastZone: ", value)

        if value then
            spawn(LastZone)
        end
    end,
})

Window:AddToggle({
    text = "Equip Best",
    flag = "toggle",
    state = false,
    callback = function(value)
        _G.Equip = value
        print("Equip: ", value)

        if value then
            spawn(Equip)
        end
    end,
})

Window:AddLabel({
    text = "YouTube: Tora IsMe",
})

Library:Init()
